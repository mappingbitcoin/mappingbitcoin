/**
 * Types barrel export
 *
 * Note: global.d.ts contains ambient declarations that are automatically
 * available throughout the project - no need to import them.
 */

// Re-export any shared types here as needed
// Example: export type { SomeType } from './someTypes';
