// Re-export everything from the seo folder for backward compatibility
export {
    default as default,
    generateCanonical,
    type PageKey,
    type Locale,
    type LocalizedMetadata,
    type SEOMap,
} from "./seo/index";
